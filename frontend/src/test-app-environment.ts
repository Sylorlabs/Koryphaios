/** SvelteKit's generated environment module is not available to the standalone
 * Vitest Vite config. Keep browser-only stores testable without changing their
 * production imports. */
export const browser = true;
export const building = false;
export const dev = true;
export const version = 'test';
