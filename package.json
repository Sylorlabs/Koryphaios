{
  "name": "koryphaios",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "bun run --filter backend dev",
    "dev:frontend": "bun run --filter frontend dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter backend test",
    "check": "bun run --filter backend typecheck && bun run --filter frontend check:strict",
    "check:full": "bun run check && bun run test"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "concurrently": "^9.1.0"
  }
}