// Configuration types for Koryphaios
// Re-export from shared to ensure consistency

export type {
  KoryphaiosConfig,
  ProviderConfig,
  MCPServerConfig,
  SafetyLimits,
  WorkspaceConfig,
  TelegramConfig,
  DiscordConfig,
  SlackConfig,
  ServerConfig,
} from '@koryphaios/shared';
