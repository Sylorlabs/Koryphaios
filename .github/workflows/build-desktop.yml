name: Build Desktop Apps

on:
  push:
    branches:
      - master
      - main
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'desktop/**'
      - 'frontend/**'
      - 'backend/**'
      - 'e2e/**'
      - 'playwright.config.ts'
      - 'package.json'
      - 'bun.lock'
      - '.github/workflows/build-desktop.yml'
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - debug

jobs:
  # Exercise the same browser-facing behavior on every desktop OS. Packaging
  # below compiles the native shell per target; this job catches platform
  # differences in the frontend/backend contract before a release is cut.
  behavior-smoke:
    name: Browser behavior (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows
            runner: blacksmith-2vcpu-windows-2025
            platform: windows
          - name: macOS
            runner: blacksmith-6vcpu-macos-latest
            platform: macos
          - name: Linux
            runner: blacksmith-2vcpu-ubuntu-2404
            platform: linux

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Playwright Chromium and Linux dependencies
        if: matrix.platform == 'linux'
        run: bunx playwright install --with-deps chromium

      - name: Install Playwright Chromium
        if: matrix.platform != 'linux'
        run: bunx playwright install chromium

      - name: Run browser end-to-end tests
        run: bun run test:e2e

  # Build for Windows
  build-windows:
    runs-on: blacksmith-2vcpu-windows-2025
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: bun install

      - name: Write release compatibility hash
        run: bun run write-compat-hash

      - name: Build frontend
        working-directory: ./frontend
        run: bun run build
        env:
          BUILD_MODE: static
          NODE_ENV: production

      - name: Compile embedded backend payload
        shell: bash
        env:
          RELAY_URL: ${{ secrets.RELAY_URL }}
        run: |
          TARGET="x86_64-pc-windows-msvc"
          OUTFILE="$GITHUB_WORKSPACE/desktop/src-tauri/embedded-backend/koryphaios-backend-${TARGET}.exe"
          mkdir -p "$(dirname "$OUTFILE")"
          # Bake the collaboration relay endpoint so shipped builds can host/join
          # out of the box (it's an endpoint, not a secret; the host secret stays
          # runtime-only). Overridable at dev time via the .env RELAY_URL.
          DEFINE=()
          [ -n "$RELAY_URL" ] && DEFINE+=(--define "process.env.RELAY_URL=\"$RELAY_URL\"")
          bun build --compile --target=bun-windows-x64 "${DEFINE[@]}" backend/src/server.ts --outfile="$OUTFILE"

      - name: Build Tauri app
        working-directory: ./desktop
        run: bun run tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v7
        with:
          name: windows-artifacts
          path: |
            desktop/src-tauri/target/release/bundle/msi/*.msi
            desktop/src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: warn

      - name: Upload to Release (if tagged)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop/src-tauri/target/release/bundle/msi/*.msi
            desktop/src-tauri/target/release/bundle/nsis/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Build for macOS (Intel and Apple Silicon)
  build-macos:
    runs-on: blacksmith-6vcpu-macos-latest
    timeout-minutes: 60
    strategy:
      matrix:
        include:
          - arch: x86_64
            target: x86_64-apple-darwin
            bun-target: bun-darwin-x64
          - arch: aarch64
            target: aarch64-apple-darwin
            bun-target: bun-darwin-arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: ${{ matrix.target }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: bun install

      - name: Write release compatibility hash
        run: bun run write-compat-hash

      - name: Build frontend
        working-directory: ./frontend
        run: bun run build
        env:
          BUILD_MODE: static
          NODE_ENV: production

      - name: Compile embedded backend payload
        shell: bash
        env:
          RELAY_URL: ${{ secrets.RELAY_URL }}
        run: |
          OUTFILE="$GITHUB_WORKSPACE/desktop/src-tauri/embedded-backend/koryphaios-backend-${{ matrix.target }}"
          mkdir -p "$(dirname "$OUTFILE")"
          DEFINE=()
          [ -n "$RELAY_URL" ] && DEFINE+=(--define "process.env.RELAY_URL=\"$RELAY_URL\"")
          bun build --compile --target=${{ matrix.bun-target }} "${DEFINE[@]}" backend/src/server.ts --outfile="$OUTFILE"

      - name: Build Tauri app (Universal Binary)
        working-directory: ./desktop
        run: |
          bun run tauri build --target ${{ matrix.target }}
        shell: bash
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: macos-artifacts-${{ matrix.arch }}
          path: |
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/macos/*.app
          if-no-files-found: warn

      - name: Upload to Release (if tagged)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Build for Linux
  build-linux:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 60
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            arch: amd64
            bun-target: bun-linux-x64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: ${{ matrix.target }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: bun install

      - name: Write release compatibility hash
        run: bun run write-compat-hash

      - name: Build frontend
        working-directory: ./frontend
        run: bun run build
        env:
          BUILD_MODE: static
          NODE_ENV: production

      - name: Compile embedded backend payload
        shell: bash
        env:
          RELAY_URL: ${{ secrets.RELAY_URL }}
        run: |
          OUTFILE="$GITHUB_WORKSPACE/desktop/src-tauri/embedded-backend/koryphaios-backend-${{ matrix.target }}"
          mkdir -p "$(dirname "$OUTFILE")"
          DEFINE=()
          [ -n "$RELAY_URL" ] && DEFINE+=(--define "process.env.RELAY_URL=\"$RELAY_URL\"")
          bun build --compile --target=${{ matrix.bun-target }} "${DEFINE[@]}" backend/src/server.ts --outfile="$OUTFILE"

      - name: Build Tauri app
        working-directory: ./desktop
        run: bun run tauri build --target ${{ matrix.target }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Verify Linux build produced expected artifacts
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        shell: bash
        run: |
          # Verify each Linux package format was produced.
          BUNDLE_DIR="desktop/src-tauri/target/${{ matrix.target }}/release/bundle"
          DEB_COUNT=$(find "$BUNDLE_DIR/deb" -name "*.deb" 2>/dev/null | wc -l)
          RPM_COUNT=$(find "$BUNDLE_DIR/rpm" -name "*.rpm" 2>/dev/null | wc -l)
          APPIMAGE_COUNT=$(find "$BUNDLE_DIR/appimage" -name "*.AppImage" 2>/dev/null | wc -l)
          echo "Artifacts: deb=$DEB_COUNT rpm=$RPM_COUNT appimage=$APPIMAGE_COUNT"
          if [ "$APPIMAGE_COUNT" -eq 0 ]; then
            echo "ERROR: No AppImage produced"
            exit 1
          fi

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v7
        with:
          name: linux-artifacts-${{ matrix.arch }}
          path: |
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/rpm/*.rpm
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
          if-no-files-found: warn

      - name: Upload to Release (if tagged)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/rpm/*.rpm
            desktop/src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Create universal macOS build (combines Intel and ARM)
  build-macos-universal:
    runs-on: blacksmith-6vcpu-macos-latest
    timeout-minutes: 60
    needs: [build-macos]
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          target: aarch64-apple-darwin

      - name: Add x86_64 target
        run: rustup target add x86_64-apple-darwin

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: bun install

      - name: Write release compatibility hash
        run: bun run write-compat-hash

      - name: Build frontend
        working-directory: ./frontend
        run: bun run build
        env:
          BUILD_MODE: static
          NODE_ENV: production

      - name: Compile universal embedded backend payload
        shell: bash
        env:
          RELAY_URL: ${{ secrets.RELAY_URL }}
        run: |
          PAYLOAD_DIR="$GITHUB_WORKSPACE/desktop/src-tauri/embedded-backend"
          mkdir -p "$PAYLOAD_DIR"
          DEFINE=()
          [ -n "$RELAY_URL" ] && DEFINE+=(--define "process.env.RELAY_URL=\"$RELAY_URL\"")
          bun build --compile --target=bun-darwin-arm64 "${DEFINE[@]}" backend/src/server.ts --outfile="$PAYLOAD_DIR/koryphaios-backend-arm64"
          bun build --compile --target=bun-darwin-x64 "${DEFINE[@]}" backend/src/server.ts --outfile="$PAYLOAD_DIR/koryphaios-backend-x64"
          lipo -create "$PAYLOAD_DIR/koryphaios-backend-arm64" "$PAYLOAD_DIR/koryphaios-backend-x64" -output "$PAYLOAD_DIR/koryphaios-backend-universal-apple-darwin"

      - name: Build Universal Binary
        working-directory: ./desktop
        run: bun run tauri build --target universal-apple-darwin
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Upload Universal macOS artifacts
        uses: actions/upload-artifact@v7
        with:
          name: macos-universal-artifacts
          path: |
            desktop/src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg
            desktop/src-tauri/target/universal-apple-darwin/release/bundle/macos/*.app
          if-no-files-found: warn

      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop/src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Summary job
  build-summary:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs: [behavior-smoke, build-windows, build-macos, build-linux]
    if: always()

    steps:
      - name: Build Summary
        run: |
          echo "Build Status Summary:"
          echo "===================="
          echo "Browser behavior: ${{ needs.behavior-smoke.result }}"
          echo "Windows: ${{ needs.build-windows.result }}"
          echo "macOS: ${{ needs.build-macos.result }}"
          echo "Linux: ${{ needs.build-linux.result }}"

          if [ "${{ needs.behavior-smoke.result }}" = "success" ] && \
             [ "${{ needs.build-windows.result }}" = "success" ] && \
             [ "${{ needs.build-macos.result }}" = "success" ] && \
             [ "${{ needs.build-linux.result }}" = "success" ]; then
            echo "All builds succeeded!"
            exit 0
          else
            echo "Some builds failed!"
            exit 1
          fi
        shell: bash
